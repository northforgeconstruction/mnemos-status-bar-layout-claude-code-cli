<!-- snap-out:brief:start -->
mnemos-status-bar-layout-claude-code-cli handoff
- Last commit: 92773ef MNEM-84: transition deploy → maintenance — v1 closed, v2 planning opens
- Where we left off: v1 shipped and live across all three surfaces; repo in maintenance phase. Found the MNEMOS hooks never engaged this session (enrolled mid-session June 11), so this project's cache is still empty.
- Blocking: nothing — project complete; the empty cache is a known cause, not a defect.
- Next action: start a fresh `claude` session in this directory so SessionStart fires and snapshots begin (needed before the June-18 MNEM-81 payload review has any data to review).
- Watching: June-18 MNEM-81 payload-size review; June-15 billing split now live — confirm the Agent SDK credit toggle at claude.ai → Settings → Billing/Usage.
<!-- snap-out:brief:end -->
